interface:
  display_name: "Storyweave"
  short_description: "Create concise, story-led presentations in ai-image or html mode"
  default_prompt: "Use $storyweave to draft the central message and page claims first, then build a Bento-native deck in either ai-image or html mode."
